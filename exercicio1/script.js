const numeroPar = Number(prompt("insira um numero par"));
const resto = numeroPar % 2;

console.log(`a sobra da divisão é ${resto}`);

// Em caso de números par, a resposta sempre será 0. Em caso de números ímpares, será diferente de zero. 